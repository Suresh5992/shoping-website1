apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: shopping

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
      - name: frontend
        image: suresh628/shop-frontend:v1
        ports:
        - containerPort: 80
